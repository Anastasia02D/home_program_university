#include <stdio.h>
#include <stdlib.h>


struct student{
        char *name;
        int group;
        float rating;
};

struct student** (FILE *file){
	struct student **s;
	while (if (fscanf(file, "%ms%d%f", &n, &g, &r)) == 3)){
		s[i]

int main (void){
	struct student **s1, **s2;
        FILE *file1 = fopen("input1.txt", "r");
        if (file1 == 0){
                printf ("Not file1\n");
                return 0;
        }
        s1 = get(file);


