#include <stdio.h>
#include <stdlib.h>


struct student{
        char *name;
        int group;
        float rating;
};

struct student ** get(FILE *file){
	struct student * = NULL;
	int err = 0;
	while (!feof(file) && err == 0)
		get_structur (

int main (void){
        struct student **s1 = NULL;
        int err;
        FILE *file1 = fopen("input.txt", "r");
        if (file1 == 0){
                printf ("Not file1\n");
                return 0;
        }
        s1 = get (file1);
